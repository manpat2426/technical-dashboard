# Copy this file to ".env" (same folder) and fill in your real values there.
# ".env" is listed in .gitignore, so it will never be committed to the repo.
# This file (.env.example) has no real secrets in it and is safe to commit.

EODHD_API_KEY=
AIRTABLE_TOKEN=
